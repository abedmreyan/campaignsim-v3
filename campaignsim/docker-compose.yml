services:
  campaignsim:
    build: .
    container_name: campaignsim
    env_file:
      - .env
    ports:
      - "3002:3002"
      - "5001:5001"
    restart: unless-stopped
    volumes:
      - ./backend/uploads:/app/backend/uploads
